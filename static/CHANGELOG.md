# 2.1.0 

## Добавлен

- Новый дизайн Material UI
- Горячие клавиши (Hotkeys) в Markdown-редакторе
- Форматирование через контекстное меню Monaco (Right-Click)
  В MonacoEditor.svelte зарегистрированы встроенные сочетания клавиш (работают на Windows/Linux Ctrl и macOS Cmd):
  - Ctrl + B — Жирный (выделенный текст)
  - Ctrl + I — Курсив (выделенный текст)
  - Ctrl + U — <u>Подчёркнутый</u> (<u>выделенный текст</u>)
  - Ctrl + Shift + X — <s>Зачёркнутый</s> (выделенный текст)
  - Ctrl + ~ (Backquote) — Инлайн код ( код )
  - Ctrl + K — Ссылка (текст)
  
  Умная отмена: Если выделенный текст уже был отформатирован (например текст), повторное нажатие hotkey снимает
  форматирование обратно в текст.

- Форматирование через контекстное меню Monaco (Right-Click)
  При клике правой кнопкой мыши по выделенному тексту в редакторе открывается контекстное меню с пунктами в группе модификации:
  - Format: Bold (text)
  - Format: Italic (text)
  - Format: Underline (<u>text</u>)
  - Format: Strikethrough (text)
  - Format: Inline Code (code)
  - Format: Link (text)
  - Format: Blockquote (> text)
  - Format: Code Block (code)


# 2.0.0

## Новые функции

- Полный перенос кодовой базы на Svelte 5 с использованием рун (`$state`, `$derived`, `$effect`)
- Переработка глобального состояния на базе реактивных классов Svelte 5
- Внедрение ленивой загрузки Monaco Editor через `@monaco-editor/loader` без необходимости встраивания веб-воркеров
- Добавлены режимы **Chat** (Чат) и **Result** (Результат) для тонкой настройки вывода
- Реализована блокировка и скрытие списков (`[LIST]`), таблиц (`[TABLE]`) и спойлеров (`[SPOILER]`) в режиме **Chat**
- Добавлена опция отключения разделительной линии (`[HR]`) с автоматическим отключением в режиме **Chat**
- Настроены пайплайны CI/CD для GitHub Actions (тестирование в `CI.yml` и деплой на GitHub Pages в `deploy.yml`)

# 1.1.0

## Новые функции

- Добавлена поддержка вложенных списков (нумерованные и маркированные)
- Добавлена поддержка нумерованных списков
- Добавлена предобработка кодовых блоков без языка (` ```code``` ` → `[CODE]`)
- Добавлены тесты для проверки конвертации
- Добавлен GitHub Actions workflow с запуском тестов перед билдом

## Исправления

- Исправлена конвертация спойлеров (`<details><summary>`)
- Исправлена конвертация inline кода (добавлены обратные кавычки)
- Исправлена обработка вложенных списков в BBCode
- Исправлен рендеринг вложенных списков в HTML
- Исправлены ошибки TypeScript в `src/stores/app.ts`

# 1.0.2

## Render Fixes

- Исправлена ошибка с лишними переносами строк в HTML рендере.
- Удален `backtick` из рендера markdown и bbcode.

# 1.0.1

- Добавлены ссылки на репозиторий GitHub и профиль создателя в заголовок.
- Добавлена система уведомлений об обновлениях с модальным окном чейнджлога.
- Улучшена разметка заголовка с использованием flexbox.
- Добавлена кнопка обновлений в хедер.
- Добавлены SEO оптимизация и улучшено title.
- Добавлена кнопка для копирования BBCode в буфер обмена. (Работает только в режиме "Добавление результата в битрикс")
